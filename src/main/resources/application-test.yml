spring:
  datasource:
    url: jdbc:tc:postgresql:16:///buildsage_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: validate

security:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret

ai:
  provider: mock
