apiVersion: v1
kind: Service
metadata:
  name: buildsage-api
  namespace: buildsage
spec:
  type: ClusterIP
  selector:
    app: buildsage-api
  ports:
    - name: http
      port: 8080
      targetPort: http
