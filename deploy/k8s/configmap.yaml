apiVersion: v1
kind: ConfigMap
metadata:
  name: buildsage-config
  namespace: buildsage
data:
  SERVER_PORT: "8080"
  AI_PROVIDER: "mock"
  CORS_ALLOWED_ORIGINS: "https://buildsage.example.com"
  TRACING_SAMPLING_PROBABILITY: "0.10"
  NOTIFICATION_WEBHOOK_ENABLED: "false"
